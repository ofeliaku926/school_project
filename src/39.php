<?php
// Initialize PHP extensions here
$extension = "all";

// Check if extensions are enabled
if ($extension) {
    echo "Extensions are enabled.";
} else {
    echo "No extensions are enabled.";
}
?>
